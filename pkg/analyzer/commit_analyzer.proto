syntax = "proto3";
option go_package = "github.com/go-semantic-release/semantic-release/pkg/analyzer";

import "pkg/semrel/structs.proto";

message AnalyzeCommits {
  message Request {
    repeated RawCommit RawCommits = 1;
  }
  message Response {
    repeated Commit Commits = 1;
  }
}

service CommitAnalyzerPlugin {
  rpc Analyze(AnalyzeCommits.Request) returns (AnalyzeCommits.Response);
}
